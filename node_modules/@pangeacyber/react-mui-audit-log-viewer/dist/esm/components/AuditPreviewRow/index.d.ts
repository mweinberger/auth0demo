import { FC } from "react";
import { Audit } from "../../types";
interface Props {
    record: Audit.FlattenedAuditRecord;
    isVerificationCheckEnabled?: boolean;
}
declare const AuditPreviewRow: FC<Props>;
export default AuditPreviewRow;
