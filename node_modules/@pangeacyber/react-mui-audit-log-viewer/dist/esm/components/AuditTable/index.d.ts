import { FC } from "react";
import { Audit } from "../../types";
import { DataGridProps, GridColDef } from "@mui/x-data-grid";
import { Sort } from "../../utils/query";
declare enum AuditTableView {
    Preview = "preview",
    Detail = "detail"
}
interface AuditTableProps {
    isVerificationCheckEnabled?: boolean;
    logs: Audit.FlattenedAuditRecord[];
    dataGridProps?: Partial<DataGridProps>;
    fields?: Partial<Record<keyof Audit.Event, Partial<GridColDef>>>;
    options?: {
        view: AuditTableView;
    };
    setSort: (sort?: Sort) => void;
}
declare const AuditTable: FC<AuditTableProps>;
export default AuditTable;
