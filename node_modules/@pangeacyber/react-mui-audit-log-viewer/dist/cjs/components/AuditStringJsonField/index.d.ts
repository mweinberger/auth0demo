import { FC } from "react";
import { Change } from "../../hooks/diff";
export declare const StringField: FC<{
    inRow?: boolean;
    title: string;
    value: string | undefined;
    changes?: Change[];
    uniqueId: string;
}>;
declare const StringJsonField: FC<{
    inRow?: boolean;
    title: string;
    value: any;
    changes?: Change[];
    uniqueId: string;
    shouldHighlight?: (c: Change) => boolean;
}>;
export default StringJsonField;
