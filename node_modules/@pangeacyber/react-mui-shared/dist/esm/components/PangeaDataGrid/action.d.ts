import { ReactNode } from "react";
import { GridColDef } from "@mui/x-data-grid";
export declare const ACTION_COLUMN = "__action__";
export declare const constructActionColumn: <T extends unknown = any>(renderActions: (object: T) => ReactNode, overrides?: Partial<GridColDef>) => GridColDef;
