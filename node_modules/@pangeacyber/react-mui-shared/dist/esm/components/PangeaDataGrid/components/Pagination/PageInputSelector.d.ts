import { FC } from "react";
interface PaginationProps {
    rowCount: number;
    paginationRowCount?: number;
    page: number;
    pageSize: number;
    onPageChange: (page: number) => void;
}
declare const PageInputSelector: FC<PaginationProps>;
export default PageInputSelector;
