import { FC } from "react";
export declare enum ShortRelativeRange {
    Year = "y",
    Month = "M",
    Week = "w",
    Day = "d",
    Hour = "h",
    Minute = "m",
    Second = "s"
}
export declare enum RelativeRange {
    Year = "year",
    Month = "month",
    Week = "week",
    Day = "day",
    Hour = "hour",
    Minute = "minute",
    Second = "second"
}
export declare const getRelativeDateRange: (range: string) => {
    amount: number | string;
    span: string;
    relativeRange: RelativeRange;
    prefix: string;
};
export declare const compareRelativeDateRanges: (aRange: string | undefined, bRange: string | undefined) => number;
export declare const getDisplayDateRange: (range: string) => string;
interface RelativeDateRangeFieldProps {
    name?: string;
    label: string;
    readonly?: boolean;
    options?: RelativeRange[];
    autoFocus?: boolean;
    variant?: "outlined";
    value: string;
    setValue: (value: string) => void;
    error?: string;
}
declare const RelativeDateRangeField: FC<RelativeDateRangeFieldProps>;
export default RelativeDateRangeField;
