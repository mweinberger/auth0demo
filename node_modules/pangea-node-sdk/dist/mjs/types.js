/**
 * Secure Audit interface definitions
 */
export var Audit;
(function (Audit) {
    let SignOptions;
    (function (SignOptions) {
        SignOptions[SignOptions["Unsign"] = 0] = "Unsign";
        SignOptions[SignOptions["Local"] = 1] = "Local";
    })(SignOptions = Audit.SignOptions || (Audit.SignOptions = {}));
})(Audit || (Audit = {}));
