import PangeaConfig from "../config.js";
import PangeaRequest from "../request.js";
import PangeaResponse from "../response.js";
declare class BaseService {
    serviceName: string;
    token: string;
    apiVersion: string;
    config: PangeaConfig;
    request: PangeaRequest;
    constructor(serviceName: string, token: string, config: PangeaConfig);
    get(endpoint: string, path: string): Promise<PangeaResponse<any>>;
    post(endpoint: string, data: object): Promise<PangeaResponse<any>>;
}
export default BaseService;
