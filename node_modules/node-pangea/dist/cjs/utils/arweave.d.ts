import { Audit } from "../types.js";
export declare type PublishedRoots = Record<number, Audit.Root>;
export declare const getArweavePublishedRoots: (treeName: string, treeSizes: number[], fetchRoot: (treeSize: number) => Promise<Audit.Root>) => Promise<PublishedRoots>;
