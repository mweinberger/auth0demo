import { Audit } from "../types.js";
import { PublishedRoots } from "./arweave.js";
export declare function verifyLogHash(envelope: Audit.EventEnvelope, hash: string): boolean;
export declare const verifyMembershipProof: ({ record, root, }: {
    record: Audit.AuditRecord;
    root: Audit.Root;
}) => boolean;
export declare const verifyConsistencyProof_: ({ newRoot, prevRoot, }: {
    record: Audit.AuditRecord;
    newRoot: Audit.Root;
    prevRoot: Audit.Root;
}) => boolean;
export declare const verifyConsistencyProof: ({ publishedRoots, record, }: {
    publishedRoots: PublishedRoots;
    record: Audit.AuditRecord;
}) => boolean;
